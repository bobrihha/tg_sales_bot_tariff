# 🛒 Telegram Sales Bot

Telegram-бот для автоматизации продаж и приема заявок.

## 📋 Функции

- **📋 Тарифы** — показ публичных и непубличных тарифов
- **🛒 Оформление заказа** — форма с именем, телефоном, email
- **💳 Оплата** — интеграция с Telegram Payments (ЮKassa, Robokassa и др.)
- **📩 Заявки** — пересылка оплаченных заявок администратору
- **❓ FAQ** — ответы на частые вопросы

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd tg_sales_bot
pip install -r requirements.txt
```

### 2. Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather) и получите токен
2. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
3. Заполните `.env` файл:
   - `BOT_TOKEN` — токен от @BotFather
   - `ADMIN_ID` — ваш Telegram ID (узнать через @userinfobot)
   - `PAYMENT_PROVIDER_TOKEN` — токен платёжного провайдера (опционально)

### 3. Запуск

```bash
python bot.py
```

## 💳 Настройка платежей

### Подключение платёжного провайдера

1. Откройте @BotFather
2. Выберите вашего бота
3. Нажмите **Payments**
4. Выберите провайдера (ЮKassa, Robokassa, Prodamus и др.)
5. Следуйте инструкциям для подключения
6. Полученный токен добавьте в `.env`

### Демо-режим

Без платёжного провайдера бот работает в демо-режиме:
- Показывает реквизиты для оплаты
- Принимает скриншоты чеков
- Пересылает заявки на проверку

## 📁 Структура проекта

```
tg_sales_bot/
├── bot.py                 # Главный файл запуска
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── .env.example           # Пример настроек
├── data/
│   └── tariffs.py         # Тарифы и товары
├── handlers/
│   ├── __init__.py        # Инициализация роутеров
│   ├── start.py           # /start и главное меню
│   ├── tariffs.py         # Выбор тарифов
│   ├── orders.py          # Оформление заказов
│   ├── payments.py        # Обработка платежей
│   └── faq.py             # FAQ
└── keyboards/
    └── main_kb.py         # Клавиатуры
```

## ⚙️ Настройка тарифов

Отредактируйте файл `data/tariffs.py`:

```python
TARIFFS = [
    Tariff(
        id="basic",
        name="🌱 Базовый",
        description="Описание тарифа",
        price=99000,  # Цена в копейках (990 руб)
        features=["✅ Функция 1", "✅ Функция 2"],
        is_public=True,  # True = публичный, False = скрытый
    ),
    # ...
]
```

## 📝 Настройка FAQ

Отредактируйте словарь `FAQ_ITEMS` в `handlers/faq.py`.

## 🔐 Безопасность

- Храните `.env` файл в безопасном месте
- Добавьте `.env` в `.gitignore`
- Никогда не публикуйте токены в открытом доступе

## 📞 Поддержка

При возникновении вопросов обращайтесь к разработчику.
